# MCP LNC Server Configuration Example
# Copy this file to .env and customize for your environment

# Default mailbox server address
# For production: mailbox.terminal.lightning.today:443
# For regtest: aperture:11110
# LNC_MAILBOX_SERVER=aperture:11110
LNC_MAILBOX_SERVER=mailbox.terminal.lightning.today:443

# Enable dev mode for local/regtest environments
LNC_DEV_MODE=true

# Skip TLS verification (only for development!)
LNC_INSECURE=true

# Connection timeout in seconds
LNC_CONNECT_TIMEOUT=30

# Local node-ops-daemon Unix socket for audit ledger queries.
NODE_OPS_DAEMON_SOCKET=~/.node-ops/daemon.sock
